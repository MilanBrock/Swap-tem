name: Java CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Create the JAR file with Maven
        run: mvn package        
      - name: Install Newman to run REST API test
        run: npm install -g newman
      - name: Start the Springboot application
        run: mvn spring-boot:run
      - name: Run Postman REST API tests
        run: newman run src/test/InventoryManagementRESTAPItest.postman_collection.json
      
